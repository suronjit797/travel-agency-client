import React from "react";
import PackagesForm from "./PackagesForm";

const CreatePackages = () => {
  return (
    <div>
      <PackagesForm mode="create" />
    </div>
  );
};

export default CreatePackages;
